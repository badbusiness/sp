<h1>Orders</h1>
<table class="table table-hover table-sm">
	<thead>
		<th>#</th>
		<th>Supplier</th>
		<th></th>
		<th></th>
	</thead>
	<tbody>
<% @orders.each do |order| %>
	<tr>
		<td><%= order.id %></td>
		<td><%= order.Supplier.name %></td>		
		<td><%= link_to :show, purchase_order_path(order)%></td>		
		<td><%= link_to :edit, edit_purchase_order_path(order)%></td>		
	</tr>
<% end %>
</tbody>
</table>

<%= link_to 'Nieuw', new_purchase_order_path %>

